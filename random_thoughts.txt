FQN

Перенес код в этот проект, но что-то брейкпоинты не работают ("No executable code found at line..."). Сегодня не стал разбираться, т.к. хотя бы сам плагин работает.

Метод createTypeByCallAndArgs либо должен быть nullable, либо нужно переписать код в точке использования. Сейчас createTypeByCallAndArgs с аннотацией @Nullable переопределяет тот же метод с аннотацией @NotNull.

Надо как-то единообразно обрабатывать случай, когда нам не удалось получить символ. Сейчас у меня разбор двух случаев - для RDotReference и для RIdentifier.